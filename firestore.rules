rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // サイトのコンテンツは誰でも読み取り可能にする
    match /site/content {
      allow read: if true;
      // 書き込みは認証済みのユーザーのみ（今回は匿名認証を利用）
      allow write: if request.auth != null;
    }
  }
}